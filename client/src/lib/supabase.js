/**
 * Supabase configuration — currently unused (we use PostgreSQL LISTEN/NOTIFY
 * via our own SSE endpoint instead). Kept for future direct Supabase usage;
 * wire these to Vite env vars (import.meta.env.VITE_SUPABASE_*) if re-enabled.
 */
export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
